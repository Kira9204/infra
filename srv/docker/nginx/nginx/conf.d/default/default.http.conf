server {
  listen 80;
  listen [::]:80;

  server_name _;
  root /var/www/default;

  location ~ /\. {
    deny all;
  }

  location / {
    try_files $uri $uri/ $uri.html =404;
  }

   access_log off;
   error_log off;
}

